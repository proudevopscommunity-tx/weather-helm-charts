apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weather-helm.fullname" . }}-test-connection"
  labels:
    {{- include "weather-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weather-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
